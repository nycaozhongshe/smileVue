import Vue from 'vue'

import { Row, Col, Button, Swipe, SwipeItem, Search, Icon, Lazyload } from 'vant';

Vue.use(Row).use(Col).use(Button).use(Swipe).use(SwipeItem).use(Search).use(Icon).use(Lazyload);
